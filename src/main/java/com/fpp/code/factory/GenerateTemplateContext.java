package com.fpp.code.factory;

import com.fpp.code.config.Environment;

public class GenerateTemplateContext extends AbstractTemplateContext {
    public GenerateTemplateContext(Environment environment) {
        super(environment);
    }
}
